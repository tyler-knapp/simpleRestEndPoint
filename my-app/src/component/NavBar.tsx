import * as React from 'react';
import {Routes, Route} from 'react-router-dom';

function NavBar() {
    return(
        <div>
            <Routes>
                <Route>

                </Route>
            </Routes>
        </div>
    )
}

export default NavBar;